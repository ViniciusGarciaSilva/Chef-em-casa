import * as React from 'react';
import MenusController from './menus.controller';

export default class MenusPage extends React.Component<any, any> {
  render() {
    return (
      <MenusController/>
    );
  }
};

